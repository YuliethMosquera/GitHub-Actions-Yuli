name: Verificación CI Básica

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  verificar:
    runs-on: ubuntu-latest

    steps:
      - name: Descargar código
        uses: actions/checkout@v4

      - name: Mostrar mensaje de verificación
        run: echo "La verificación CI se ejecutó correctamente"

      - name: Revisar archivos del proyecto
        run: ls